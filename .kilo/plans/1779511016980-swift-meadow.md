# Admin Functionality Enhancement Plan

## Goal
Add admin functionality for the user with email: madinabonuqoraboyeva1@gmail.com

## Current State
- The project already has admin views implemented in `src/components/AdminViews.tsx`
- Role-based routing exists in `src/App.tsx`
- Admin email lists are defined in `src/lib/constants.ts`

## Admin Configuration Locations
1. **Email lists**: `src/lib/constants.ts`
   - `SUPER_ADMIN_EMAILS`: ['admin@native.edu']
   - `ADMIN_EMAILS`: ['osiyo@native.edu', 'sarvar@native.edu']

2. **Role determination logic**: `src/App.tsx` (lines 25-52)
   - Checks if user email is in SUPER_ADMIN_EMAILS → role = 'super-admin'
   - Checks if user email is in ADMIN_EMAILS → role = 'admin'
   - Maps admin emails to teacher IDs via ADMIN_TEACHER_MAP

## Changes Needed
1. Add `madinabonuqoraboyeva1@gmail.com` to the `ADMIN_EMAILS` array in `src/lib/constants.ts`
2. Add a teacher ID mapping for this email in `ADMIN_TEACHER_MAP` in `src/lib/constants.ts`

## Implementation Details
- Use teacher ID `t1` for this admin (assuming it's available based on existing mappings t2, t3)
- No changes needed to admin views as they already support admin/super-admin roles
- The existing role determination logic in `src/App.tsx` will automatically grant admin access

## Files to Modify
- `src/lib/constants.ts`

## Implementation Instructions (to be executed after exiting plan mode)
1. Edit `src/lib/constants.ts`:
   - Add `'madinabonuqoraboyeva1@gmail.com',` to the ADMIN_EMAILS array
   - Add `'madinabonuqoraboyeva1@gmail.com': 't1',` to the ADMIN_TEACHER_MAP object

## Testing
After implementation, the user should be able to:
1. Sign in with email madinabonuqoraboyeva1@gmail.com
2. Access admin dashboard views
3. Use all admin functionality (booking management, reports, etc.)

## Status
Ready for implementation - changes need to be made to src/lib/constants.ts